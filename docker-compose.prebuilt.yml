version: '3.8'

services:
  # MongoDB Database
  mongodb:
    image: mongo:7
    container_name: profilebuilder-mongo
    restart: always
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD:-ProfileBuilder2025Secure!}
    networks:
      - profilebuilder-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/admin -u admin -p ${MONGO_PASSWORD:-ProfileBuilder2025Secure!} --quiet
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache (optional)
  redis:
    image: redis:7-alpine
    container_name: profilebuilder-redis
    restart: always
    volumes:
      - redis_data:/data
    networks:
      - profilebuilder-network

  # Backend API (pre-built)
  backend:
    image: node:20-alpine
    container_name: profilebuilder-backend
    restart: always
    working_dir: /app
    ports:
      - "${PORT:-5000}:5000"
    environment:
      NODE_ENV: production
      PORT: 5000
      MONGODB_URI: mongodb://admin:${MONGO_PASSWORD:-ProfileBuilder2025Secure!}@mongodb:27017/profilebuilder?authSource=admin
      REDIS_URL: redis://redis:6379
      JWT_SECRET: ${JWT_SECRET:-ProfileBuilder-JWT-Secret-Key-2025-Production-Env}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:3000}
    volumes:
      - ./backend/dist:/app/dist:ro
      - ./backend/package.json:/app/package.json:ro
      - ./backend/node_modules:/app/node_modules:ro
      - ./scripts:/app/scripts:ro
      - uploads_data:/app/uploads
    command: node dist/server.js
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - profilebuilder-network

  # Frontend (pre-built Next.js)
  frontend:
    image: node:20-alpine
    container_name: profilebuilder-frontend
    restart: always
    working_dir: /app
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:5000}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-http://localhost:3000}
    volumes:
      - ./frontend/.next:/app/.next:ro
      - ./frontend/public:/app/public:ro
      - ./frontend/package.json:/app/package.json:ro
      - ./frontend/node_modules:/app/node_modules:ro
      - ./frontend/next.config.js:/app/next.config.js:ro
    command: node_modules/.bin/next start -p 3000
    depends_on:
      - backend
    networks:
      - profilebuilder-network

volumes:
  mongo_data:
  redis_data:
  uploads_data:

networks:
  profilebuilder-network:
    driver: bridge
